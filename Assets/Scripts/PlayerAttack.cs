using UnityEngine;

public class PlayerAttack : MonoBehaviour
{
    //what is the player
    //what is the enemy
    //what is colliding 
    //reference stat script
    //reference Regular UI script 

    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        //check if user presses attack button
            //add punch animation
            //does it touch the enemy
                //if yes take away enemy health
                //if no do nothing
    }
}
